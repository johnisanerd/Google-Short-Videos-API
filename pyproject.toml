[project]
name = "apify-google-short-videos-api"
version = "0.1.0"
description = "Example client for the Google Short Videos API Apify Actor"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "apify-client>=1.4.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
